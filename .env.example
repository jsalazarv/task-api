# Database Configuration (used by Docker Compose and Prisma)
# Note: Update DATABASE_URL if you change any of the POSTGRES_* variables
POSTGRES_USER=hometasks
POSTGRES_PASSWORD=hometasks_dev_password
POSTGRES_DB=hometasks_dev
DATABASE_URL="postgresql://hometasks:hometasks_dev_password@localhost:5432/hometasks_dev?schema=public"

# pgAdmin Configuration (used by Docker Compose)
PGADMIN_EMAIL=admin@hometasks.local
PGADMIN_PASSWORD=admin

# Application
NODE_ENV=development
PORT=3000

# JWT (to be configured later in auth module)
JWT_ACCESS_SECRET=change-this-secret-in-production-use-openssl-rand-base64-32
JWT_REFRESH_SECRET=change-this-refresh-secret-in-production-use-openssl-rand-base64-32
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
